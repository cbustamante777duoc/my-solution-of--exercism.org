function invertirCadena(cadena) {
    // Tu código aquí
}

const cadena = "Hola mundo";
const cadenaInvertida = invertirCadena(cadena);
console.log(cadenaInvertida); // odnum aloH

function contarVocales(cadena) {
    // Tu código aquí
}


const numeroVocales = contarVocales(cadena);
console.log(numeroVocales); // 4

function eliminarEspacios(cadena) {
    // Tu código aquí
}


const cadenaSinEspacios = eliminarEspacios(cadena);
console.log(cadenaSinEspacios); // Holamundo

function convertirMayusculas(cadena) {
    // Tu código aquí
}

function convertirMinusculas(cadena) {
    // Tu código aquí
}


const cadenaMayusculas = convertirMayusculas(cadena);
const cadenaMinusculas = convertirMinusculas(cadena);
console.log(cadenaMayusculas); // HOLA MUNDO
console.log(cadenaMinusculas); // hola mundo

function buscarSubcadena(cadena, subcadena) {
    // Tu código aquí
}


const subcadena = "mundo";
const indice = buscarSubcadena(cadena, subcadena);
console.log(indice); // 6


function extraerSubcadena(cadena, indiceInicial, indiceFinal) {
    // Tu código aquí
}


const indiceInicial = 0;
const indiceFinal = 4;
const subcadenaExtraida = extraerSubcadena(cadena, indiceInicial, indiceFinal);
console.log(subcadenaExtraida); // Hola


function validarEmail(email) {
    // Tu código aquí
}

const email = "correo@ejemplo.com";
const emailValido = validarEmail(email);
console.log(emailValido); // true

function convertirANumero(cadena) {
    // Tu código aquí
}

const cadenaNumero = "1234";
const numero = convertirANumero(cadena);
console.log(numero); // 1234

function formatearFecha(fecha) {
    // Tu código aquí
}

const fecha = new Date();
const fechaFormateada = formatearFecha(fecha);
console.log(fechaFormateada); // dd/mm/aaaa




