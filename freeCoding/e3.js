/**
 * function that union two arrays
 * @param {*} arr1 data array
 * @param {*} arr2 data array
 * @returns union two arrays
 */
const myConcat = (arr1, arr2) => arr1.concat(arr2);

console.log(myConcat([1, 2], [3, 4, 5]));